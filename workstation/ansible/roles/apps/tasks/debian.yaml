---
- name: Adicionando chave Brave
  ansible.builtin.apt_key:
    url: 'https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg'
    keyring: /usr/share/keyrings/brave-browser-archive-keyring.gpg
    state: present
- name: Adicionando Repositorio Brave
  ansible.builtin.apt_repository:
    repo: 'deb [arch=amd64 signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main'
    state: present
    filename: brave
- name: Instalando Brave
  ansible.builtin.apt:
    name: brave-browser

- name: Instalando OnlyOffice
  ansible.builtin.apt:
    deb: '{{onlyoffice_urll}}'

- name: Instalando Bitwarden
  ansible.builtin.apt:
    deb: '{{bitwarden_url}}'

- name: Adicionando Repositorio PPA Eve-Integration
  ansible.builtin.apt_repository:
    repo: 'ppa:smartfinn/eve-ng-integration'
    codename: '{{ansible_distribution_release}}'

- name: Instalando Eve-Integration
  ansible.builtin.apt:
    name: eve-ng-integration

...